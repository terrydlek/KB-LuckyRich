<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.e.dao.UserDao">
    <insert id="register" parameterType="mul.cam.e.security.SecurityUser">
        insert into user(user_name, email, provider_id)
        values (#{user_name}, #{email}, #{provider_id})
    </insert>

    <select id="getAccountNum" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(ua.account_id)
        FROM account ua
                 JOIN user u ON ua.user_id = u.user_id
        WHERE u.email = #{email}
    </select>

    <select id="getUserDtoByEmail" resultType="mul.cam.e.security.SecurityUser">
        select user_id, user_name, email, gender, age, provider_id
        from user
        where email=#{email}
    </select>

    <update id="updateUserInfo">
        UPDATE user
        SET age=#{request.age}, gender=#{request.gender}
        WHERE email=#{email}
    </update>
</mapper>
