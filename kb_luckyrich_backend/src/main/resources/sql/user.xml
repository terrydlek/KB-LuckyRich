<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.e.dao.UserDao">
    <select id="userCheck" parameterType="java.lang.String" resultType="java.lang.Integer">
        select count(*)
        from user
        where email=#{email}
    </select>

    <insert id="register" parameterType="mul.cam.e.dto.UserDto">
        insert into user(user_name, email, provider_id)
        values (#{user_name}, #{email}, #{provider_id})
    </insert>

    <select id="login" parameterType="java.lang.String" resultType="mul.cam.e.security.UserDetail">
        select user_name, email, gender, age
        from user
        where email=#{email}
    </select>

    <select id="getUserByEmail" parameterType="java.lang.String" resultType="mul.cam.e.security.UserDetail">
        select user_name, email, gender, age
        from user
        where email=#{email}
    </select>

    <select id="getAccountNum" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT COUNT(ua.account_id)
        FROM account ua
                 JOIN user u ON ua.user_id = u.user_id
        WHERE u.email = #{email}
    </select>
    <select id="loginByEmailAndProvider" parameterType="map" resultType="mul.cam.e.security.UserDetail">
        select user_name, email, gender, age
        from user
        where email = #{email} and provider_id = #{provider_id}
    </select>

    <update id="updateUserInfo">
        UPDATE user
        SET age=#{request.age}, gender=#{request.gender}
        WHERE email=#{email}
    </update>
</mapper>
