<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mul.cam.e.dao.DepositDao">

    <select id="getDeposit" resultType="mul.cam.e.dto.DepositDto">
        select *
        from deposit_data;
    </select>
</mapper>