<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mul.cam.e.dao.BoardDao">

    <select id="getBoardList" resultType="mul.cam.e.dto.BoardDto">
        select a.board_num, a.user_name, b.nick_name, a.title, a.content, a.created_at
        from board a join user b on a.user_name = b.user_name
        where del = 0
        order by created_at desc;
    </select>

    <insert id="insertBoard" parameterType="mul.cam.e.dto.BoardDto">
        insert into board(user_name, title, content, created_at)
        values (#{userName}, #{title}, #{content}, #{createdAt});
    </insert>

    <select id="getBoard" parameterType="int" resultType="mul.cam.e.dto.BoardDto">
        select a.board_num, a.user_name, b.nick_name, a.title, a.content, a.created_at
        from board a join user b on a.user_name = b.user_name
        where a.board_num = #{boardNum};
    </select>

<!--    <delete id="deleteBoard" parameterType="int">-->
<!--        delete-->
<!--        from board-->
<!--        where board_num = #{boardNum};-->
<!--    </delete>-->

    <update id="deleteBoard" parameterType="int">
        update board
        set del = 1
        where board_num = #{boardNum};
    </update>

    <update id="updateBoard" parameterType="mul.cam.e.dto.BoardDto">
        update board
        set content = #{content}
        where board_num = #{boardNum};
    </update>

    <insert id="addComment" parameterType="mul.cam.e.dto.BoardReplyDto">
        insert into board_reply(board_num, reply, reply_at)
        values(#{boardNum}, #{reply}, #{replyAt});
    </insert>

    <select id="getReply" parameterType="int" resultType="mul.cam.e.dto.BoardReplyDto">
        select reply, reply_at
        from board_reply
        where board_num = #{boardNum}
        order by 2;
    </select>

</mapper>