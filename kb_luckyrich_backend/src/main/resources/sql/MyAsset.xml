<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.e.dao.MyAssetDao">

    <select id="getAccounts" resultType="mul.cam.e.dto.AccountDTO">
        SELECT a.account_id, a.account_name, a.account_number, a.balance, a.account_type
        FROM account a
            JOIN user_account ua ON a.account_id = ua.account_id
        WHERE ua.user_id = #{userId}

    </select>
</mapper>